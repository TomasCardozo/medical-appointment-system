spring:
  application:
    name: doctor-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

server:
  port: ${DOCTOR_SERVICE_PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info
