server:
  port: 8888

spring:
  cloud:
    config:
      server:
        native:
          search-locations: ${CONFIG_REPO_PATH:file:../../infrastructure/config-repo}
  profiles:
    active: native

management:
  endpoints:
    web:
      exposure:
        include: health,info
